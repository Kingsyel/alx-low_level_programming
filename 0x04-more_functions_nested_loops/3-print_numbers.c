#include "main.h"
/**
 * print_numbers - prints the numbers,followed by new line
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
	int a;

	if (a = 0; a < 10; a++)
	{ _puthcar (a);
	}
	_putchar ('\n');
}
